# Team B：工作台怎么改的

这是独立竞品方案，**没有合进 main**。Team A 每天一把刀的改动、以及 Venice 草稿 #132，都没动。

## 我们觉得现在哪里别扭（可推翻）

下面是假设，不是定论。如果你用下来觉得假设错了，直接丢掉这一支即可。

1. **空窗太小。** 第一次打开，真正该做的事是「把文件夹拖进来」，却挤在左边一条窄栏里。中间两大块黑预览、右边曝光/白平衡，都还没轮到。
2. **步骤看不见。** 产品其实就三步：导入 → 每条选出成对 Log 与色域 → 处理已锁定片段。旧界面把这三步写成一行小字，和按钮、高级、状态栏挤在一起，不知道现在该点哪。
3. **预览输出藏太深。** 709 / HLG / PQ / 关，是每天都要切的，旧界面几乎只在「设置」里。中间预览换了，人却找不到开关。
4. **高级和主路径抢位置。** 节点条、达芬奇包导出不该和「处理已锁定片段」并肩。该收着。

若你其实更在意「三个栏一直都在、空窗也要看到列表」，那第 1 条就错了，空窗全幅可以不要。

## 现在怎么走

```
空窗（还没素材）
┌──────────────────────────────────────────────┐
│  LogBridge          ①导入  ②选对  ③写出代理   │
│                     预览输出：709 / HLG / PQ   │
├──────────────────────────────────────────────┤
│                                              │
│          把混源文件夹拖进来（或点这里选）      │
│     1 导入     2 选对     3 写出代理           │
│                                              │
└──────────────────────────────────────────────┘

有素材之后
┌──────────────────────────────────────────────┐
│  顶栏：三步进度 + 预览输出（预览·非成片）       │
├────────┬──────────────────────────┬──────────┤
│ 素材   │  大预览（源 | 预览）       │ 调节     │
│ 待选/  │  这一条：成对 IDT          │ 曝光     │
│ 已锁定 │  写出代理 / 处理已锁定片段 │ 白平衡   │
│        │  高级（默认收起）          │          │
└────────┴──────────────────────────┴──────────┘
```

```mermaid
flowchart LR
    A[导入混源文件夹] --> B[每条选成对 Log 与色域]
    B --> C[处理已锁定片段]
    C --> D[得到 EXR 图序列<br/>不是视频]
```

## 相对旧界面，改了什么

- **空窗变成整块画布**，不再先甩出三栏空壳。
- **顶栏写出三步**，当前步会亮起来。
- **预览输出放到顶栏**，不用再钻设置。
- **待选时，成对 IDT 那一条会醒目**，告诉你「现在该选对」。
- **处理已锁定片段** 仍只在有已锁定条目时出现；没锁上时用中文说明为什么还不能写。
- **高级仍默认收起**，节点和「导出 ACEScct / EXR」还在那里。
- **左边可按 全部 / 待选 / 已锁定 看列表**，方便混源箱。

没改的：成对 IDT 必须人手选、不猜；写出的是代理 EXR 图序列；Venice 对仍只在检测到时出现；颜色公式、达芬奇包内容都没动。

## 还没做完 / 请当没验过

- 这台云电脑不是 Mac，**没有在 Xcode 里编过，也没有对着达芬奇点过**。CI 绿不等于达芬奇已验证。
- 高级仍是收起的一条，没有做成单独窗口。
- 没有做缩略图、没有做更复杂的时间线。
- 请在自己的 Mac 上：打开工程 → 运行 → 拖自己的混源文件夹 → 选对 → 点处理已锁定片段，看空窗和三步是否真的更顺。
